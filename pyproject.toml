[project]
name = "custcodian-website"
version = "0.2.0"
description = "Website for custcodian.dev"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[dependency-groups]
dev = [
    "zensical>=0.0.65",
]
